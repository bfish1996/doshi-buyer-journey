import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Doshi Buyer Journey Impact',
  description: 'Interactive visualization of Doshi\'s impact across retail banking buyer journeys',
  keywords: 'Doshi, financial services, buyer journey, banking, fintech',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
